#include <stdio.h>

/* Supply parentheses to show how a C compiler would interpret each of the following expressions. */

int main()
{
    printf("a) ((a * b) - (c * d)) + e\n");
    printf("b) ((a / b) %% c) / d\n");
    printf("c) (((- a) - b) + c) - (+ d)\n");
    printf("d) ((a * (-b) / c) - d\n");
}
