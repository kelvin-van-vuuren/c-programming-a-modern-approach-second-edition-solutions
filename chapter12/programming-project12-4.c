#include <stdio.h>

/* Simplify programming project 2 by taking advantage of the fact that an array name can be used as a pointer
 */

int main() { printf("Solution to programming project 12-2 already makes use of array name as a pointer.\n"); }
