#include <stdio.h>

/* Simplify programming project 1 by taking advantage of the fact that an array name can be used as a pointer
 */

int main() { printf("Solution to programming project 12-1 already makes use of array name as a pointer.\n"); }
