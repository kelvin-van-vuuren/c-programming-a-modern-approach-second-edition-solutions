#include <stdio.h>

/* Why is it not a good idea for an identifier to contain more than one adjacent underscore (as in current___balance,
 * for example)? */

int main()
{
    printf(
        "It can be difficult to determine how many adjacent underscores are present when there is more than one. This "
        "reduces the program's readability and therefore is not good practice.\n");
}
