#include <stdio.h>

/* How many tokens are there in the following statement?:
 * answer=(3*q-p*p)/3;
 */

int main()
{
    printf(
        "C programs can be thought of as a series of tokens: groups of characters that can't be split up without "
        "changing their meaning.\n");
    printf("The statement 'answer=(3*q-p*p);' is consists of 12 tokens.\n");
}
