#include <stdio.h>

/* Suppose that we call scanf as follows:
 * scanf("%d%f%d" &i, &x, &j);
 * If the user enter:
 * 10.3 5 6
 * what will the values of i, x and j be after the call? (assume i and j are int variables and x is a float variable).
 */

int main() { printf("i = 10, x = .3, j = 5\n"); }
